name: "Construction — Code plan override on review FAIL"
orchestrator: aidlc-construction-orchestrator
inputs:
  complexity: Standard
  choices:
    units-generation: B
    code-plan: C
    code-generation-result: enter
    build-and-test-result: A
expect:
  stage_path:
    - context-load
    - revalidation
    - stage-decision
  final_stage: stage-decision
